<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary Source="ResourceDictionary.xaml"/>
    </UserControl.Resources>

    <Grid Background="White">
        <TabControl Margin="10" x:Name="MainTabControl">
            
            <TabItem Header="Все запчасти" FontFamily="Roboto" FontSize="14" Height="30">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Vertical" Grid.Row="0"  VerticalAlignment="Center">
                        <TextBlock Text="Общий список" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="16" FontFamily="Roboto" Foreground="{StaticResource ColorTextPrimary}"/>
                        <TextBox x:Name="SearchBar" Style="{StaticResource TextBoxStyle}" Height="35" Margin="10" Tag="Поиск..."/>
                    </StackPanel>

                    <DataGrid x:Name="AllPartsDataGrid" Grid.Row="1" Margin="10,0,10,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Артикул" Binding="{Binding Sku}" Width="120"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Производитель" Binding="{Binding BrandName}" Width="150"/>
                            <DataGridTextColumn Header="Остаток" Binding="{Binding Stock}" Width="80"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding RetailPrice, StringFormat={}{0:C}}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="По авто" FontFamily="Roboto" FontSize="14">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="10,0" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnBack" Content="Назад" Width="100" Height="30" Margin="0,0,15,0" Style="{DynamicResource MainButtonStyle}" Visibility="Collapsed"/> 
                        <TextBlock x:Name="TxtBreadcrumb" Text="Выберите марку" VerticalAlignment="Center" FontSize="16" Foreground="{StaticResource ColorTextPrimary}"/>
                    </StackPanel>

                    <ListBox x:Name="NavigationList" Grid.Row="1" 
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                             BorderThickness="0" Background="Transparent"
                             ItemContainerStyle="{StaticResource TileItemContainerStyle}"
                             ItemTemplate="{StaticResource TextTileTemplate}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>

                    <DataGrid x:Name="CarPartsGrid" Grid.Row="1" Margin="10,0,10,10" Visibility="Collapsed">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Артикул" Binding="{Binding Sku}" Width="120"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Производитель" Binding="{Binding BrandName}" Width="150"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding RetailPrice}" Width="100"/>
                            <DataGridTextColumn Header="Остаток" Binding="{Binding Stock}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="По категориям" FontFamily="Roboto"  FontSize="14">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="CatBtnBack" Content="Назад" Width="100" Height="30" Margin="0,0,15,0" Style="{DynamicResource MainButtonStyle}" Visibility="Collapsed"/>
                        <TextBlock x:Name="CatTxtBreadcrumb" Text="Выберите категорию" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="16" Foreground="{StaticResource ColorTextPrimary}"/>
                    </StackPanel>
                    <ListBox x:Name="CatNavigationList" Grid.Row="1" 
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                             BorderThickness="0" Background="Transparent"
                             ItemContainerStyle="{StaticResource TileItemContainerStyle}"
                             ItemTemplate="{StaticResource TextTileTemplate}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                    <DataGrid x:Name="CatPartsGrid" Grid.Row="1" Visibility="Collapsed">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Артикул" Binding="{Binding Sku}" Width="120"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Производитель" Binding="{Binding BrandName}" Width="150"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding RetailPrice}" Width="100"/>
                            <DataGridTextColumn Header="Остаток" Binding="{Binding Stock}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>