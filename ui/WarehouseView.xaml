<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Склад запчастей" 
                       FontSize="24" FontWeight="SemiBold" 
                       Foreground="#2C3E50" Margin="0,0,15,0"/>
            
            <Button Content="Приход товара" 
                    Style="{DynamicResource MainButtonStyle}" 
                    Width="130" Height="35"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Style="{DynamicResource MainDataGridStyle}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" Width="100"/>
                <DataGridTextColumn Header="Название" Width="*"/>
                <DataGridTextColumn Header="Производитель" Width="150"/>
                <DataGridTextColumn Header="Остаток" Width="80"/>
                <DataGridTextColumn Header="Цена" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>